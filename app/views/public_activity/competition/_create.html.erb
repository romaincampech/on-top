created the competitition <%= link_to activity.trackable.name, activity.trackable %> -
<%= activity.trackable.created_at.strftime("%d %B %Y") %> @ <%= activity.trackable.created_at.strftime("%H:%M") %>
